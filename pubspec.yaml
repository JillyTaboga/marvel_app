name: marvel_app
description: Um app para acompanhar os personagens Marvel.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.18.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.6
  hooks_riverpod: ^2.1.3
  flutter_hooks: ^0.18.5+1
  json_annotation: ^4.7.0
  freezed_annotation: ^2.2.0
  intl: ^0.18.0
  crypto: ^3.0.2
  convert: ^3.1.1
  sensors_plus: ^2.0.1
  transparent_image: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  freezed: ^2.3.2
  json_serializable: ^6.5.4

flutter:
  uses-material-design: true
